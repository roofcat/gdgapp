<%= form_for([commentable, @comment], remote: true, html: {id:"comments-form", :"data-type" => "json"}) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.text_area :body, placeholder: 'Déjanos tu comentario', class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Enviar comentario', class:'btn btn-xs btn-success' %>
  </div>
<% end %>